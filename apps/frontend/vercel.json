{
  "buildCommand": "cd ../.. && pnpm install && pnpm --filter @phishguard/shared build && pnpm --filter frontend build",
  "outputDirectory": "dist",
  "framework": "vite",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
